#First Month Day examples
# to get the first day of the current month use:
firstMonthDay(Sys.time())
# to get the twenty fifteen January first use:
firstMonthDay(as.Date("2015-01-18"))
#can replace the 18 for any number
